{
  "name": "myapp",
  "version": "1.0.0",
  "description": "NodeJS Typescript Lambda",
  "author": "SAM CLI",
  "license": "MIT",
  "scripts": {
    "deploy": "cd myapp && npm i && npm run build && npm run deploy"
  }
}